```sql
SELECT * FROM employees WHERE department = 'Sales' AND salary >= 100000;
```
This corrected query includes employees with salaries greater than or equal to 100000, ensuring all relevant employees are included in the result set.